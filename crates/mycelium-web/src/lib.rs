//! Mycelium Web Frontend — Leptos-based UI.
//!
//! Single-page application compiled to WASM with server-side rendering.
//! Replaces the existing React TypeScript frontend.

pub fn placeholder() -> &'static str {
    "mycelium-web"
}
