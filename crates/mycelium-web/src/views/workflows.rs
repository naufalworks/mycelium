//! Workflows view — library, runs, and run details.

use leptos::prelude::*;

/// Workflow library and run management.
#[component]
pub fn WorkflowsView() -> impl IntoView {
    view! {
        <div class="view workflows-view">
            <h2>"Workflows"</h2>
            <div class="placeholder-card">
                <p>"Workflows will appear here..."</p>
            </div>
        </div>
    }
}
